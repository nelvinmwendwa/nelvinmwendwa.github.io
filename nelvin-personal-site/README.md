# Nelvin Mwendwa — personal site

Static HTML/CSS site. No framework, build step, server or database required.

## Preview locally
Open `index.html` in a browser, or run:
`python3 -m http.server 8000`
then visit `http://localhost:8000`.

## Before publishing
- Confirm the live URLs for the two Render projects.
- Add a favicon if desired.
- Replace any project wording that has changed.
- Add Google Search Console after publishing and submit the sitemap if a domain is available.
